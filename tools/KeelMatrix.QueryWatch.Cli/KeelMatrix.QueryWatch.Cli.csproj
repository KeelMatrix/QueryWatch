<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <UseAppHost>false</UseAppHost>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>false</TreatWarningsAsErrors>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>

    <!-- Public qwatch .NET tool package -->
    <IsPackable>true</IsPackable>
    <PackAsTool>true</PackAsTool>
    <ToolCommandName>qwatch</ToolCommandName>
    <PackageId>qwatch</PackageId>
    <Authors>KeelMatrix</Authors>
    <Description>Detect N+1 query regressions and enforce database and SQL performance budgets in CI from QueryWatch JSON summaries.</Description>
    <PackageTags>querywatch;dotnet-tool;cli;database-testing;sql-performance;n-plus-one;nplusone;ef-core;ci;budgets</PackageTags>
    <RepositoryUrl>https://github.com/KeelMatrix/QueryWatch</RepositoryUrl>
    <PackageProjectUrl>https://github.com/KeelMatrix/QueryWatch#cli</PackageProjectUrl>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageIcon>icon.png</PackageIcon>
    <RepositoryType>git</RepositoryType>
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <EmbedUntrackedSources>true</EmbedUntrackedSources>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>

    <!-- Deterministic builds -->
    <Deterministic>true</Deterministic>
    <ContinuousIntegrationBuild>true</ContinuousIntegrationBuild>

    <!-- Safe trimming only when publishing self-contained -->
    <EnableCliTrim Condition="'$(Configuration)'=='Release' and '$(EnableCliTrim)'==''">true</EnableCliTrim>
    <PublishTrimmed Condition="'$(EnableCliTrim)'=='true' and '$(SelfContained)'=='true'">true</PublishTrimmed>
    <TrimMode Condition="'$(EnableCliTrim)'=='true' and '$(SelfContained)'=='true'">partial</TrimMode>
    <InvariantGlobalization>true</InvariantGlobalization>
    <NoWarn>$(NoWarn);NU5128</NoWarn>

    <GeneratePackageOnBuild>false</GeneratePackageOnBuild>
  </PropertyGroup>

  <ItemGroup>
    <!-- Keep Contracts non-packable and compile its shared source into qwatch. -->
    <Compile Include="..\..\src\KeelMatrix.QueryWatch.Contracts\QueryWatchContracts.cs" Link="Contracts\QueryWatchContracts.cs" />
    <Compile Include="..\..\src\KeelMatrix.QueryWatch.Contracts\QueryWatchJsonContext.cs" Link="Contracts\QueryWatchJsonContext.cs" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="KeelMatrix.Telemetry" />
  </ItemGroup>

  <!-- PackAsTool suppresses dependencies by default; retain the published Telemetry dependency in qwatch.nuspec. -->
  <Target Name="EnableQwatchPackageDependencies"
          BeforeTargets="_GetFrameworksWithSuppressedDependencies"
          Condition="'$(PackAsTool)' == 'true'">
    <PropertyGroup>
      <SuppressDependenciesWhenPacking>false</SuppressDependenciesWhenPacking>
    </PropertyGroup>
  </Target>
</Project>
